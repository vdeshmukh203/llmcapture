(function(){'use strict';const $=id=>document.getElementById(id);const captureStatus=$('captureStatus'),platformName=$('platformName'),messageCount=$('messageCount'),promptCount=$('promptCount'),sessionIdEl=$('sessionId'),lockReason=$('lockReason'),activeTabUrl=$('activeTabUrl'),captureToggle=$('captureToggle'),rawInputToggle=$('rawInputToggle'),autoRefreshToggle=$('autoRefreshToggle'),refreshBtn=$('refreshStatus'),forceCapture=$('forceCapture'),sessionList=$('sessionList'),exportAll=$('exportAll'),verifyAll=$('verifyAll'),clearAll=$('clearAll'),verifyModal=$('verifyModal'),verifyResults=$('verifyResults'),closeModal=$('closeModal');let timer=null;async function activeTab(){const [tab]=await chrome.tabs.query({active:true,currentWindow:true});return tab;} function setInactive(msg){captureStatus.textContent=msg||'Inactive';captureStatus.className='status-badge status-inactive';platformName.textContent='-';messageCount.textContent='0';promptCount.textContent='0';sessionIdEl.textContent='-';lockReason.textContent='-';} async function refreshStatus(){try{const tab=await activeTab();activeTabUrl.textContent=tab?.url||'-';if(!tab){setInactive('Inactive');return;} chrome.tabs.sendMessage(tab.id,{type:'GET_STATUS'},res=>{if(chrome.runtime.lastError||!res){setInactive('Inactive');return;} if(res.locked){captureStatus.textContent='Locked';captureStatus.className='status-badge status-inactive';} else if(res.isCapturing){captureStatus.textContent='Active';captureStatus.className='status-badge status-active';} else {captureStatus.textContent='Paused';captureStatus.className='status-badge status-inactive';} platformName.textContent=res.platform||'-';messageCount.textContent=String(res.messageCount||0);promptCount.textContent=String(res.promptCount||0);sessionIdEl.textContent=res.sessionId||'-';lockReason.textContent=res.lockReason||'-';});}catch(e){setInactive('Error');}} async function refreshSessionList(){const sessions=await SessionStorage.getAllSessions();if(!sessions.length){sessionList.innerHTML='<div class="empty-state">No sessions yet</div>';return;} sessions.sort((a,b)=>new Date(b.startedAt)-new Date(a.startedAt));sessionList.innerHTML=sessions.map(s=>`<div class='session-item'><div><div class='session-platform'>${s.platform}</div><div class='session-meta'>${s.promptCount||0} prompts · ${s.entryCount||0} msgs</div></div><div class='session-actions'><button class='session-action-btn export' data-id='${s.sessionId}'>Export</button></div></div>`).join('');document.querySelectorAll('.export').forEach(b=>b.onclick=()=>exportSession(b.dataset.id));} function downloadJson(filename,data){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url);} async function exportSession(id){const exported=await SessionStorage.exportSession(id);if(!exported)return;const p=exported.forensicLog.session.platform;downloadJson(`ai-capture-${p}-${id}.json`,exported.forensicLog);downloadJson(`ai-capture-${p}-${id}-manifest.json`,exported.studyManifest);} async function refreshAll(){await refreshStatus();await refreshSessionList();} function startTimer(){clearInterval(timer);if(autoRefreshToggle.checked){timer=setInterval(refreshAll,2000);}} async function init(){const settings=await SessionStorage.getSettings();captureToggle.checked=settings.captureEnabled;rawInputToggle.checked=settings.captureRawInput;autoRefreshToggle.checked=true;await refreshAll();startTimer();} refreshBtn.onclick=refreshAll;autoRefreshToggle.onchange=startTimer;captureToggle.onchange=async()=>{const s=await SessionStorage.getSettings();s.captureEnabled=captureToggle.checked;await SessionStorage.saveSettings(s);const tab=await activeTab();if(tab)chrome.tabs.sendMessage(tab.id,{type:'TOGGLE_CAPTURE',enabled:captureToggle.checked});await refreshAll();};rawInputToggle.onchange=async()=>{const s=await SessionStorage.getSettings();s.captureRawInput=rawInputToggle.checked;await SessionStorage.saveSettings(s);};forceCapture.onclick=async()=>{const tab=await activeTab();if(tab)chrome.tabs.sendMessage(tab.id,{type:'FORCE_CAPTURE'},()=>refreshAll());};exportAll.onclick=async()=>{const sessions=await SessionStorage.getAllSessions();for(const s of sessions)await exportSession(s.sessionId);};verifyAll.onclick=async()=>{const sessions=await SessionStorage.getAllSessions();let html='';for(const s of sessions){const session=await SessionStorage.getSession(s.sessionId);if(!session)continue;const result=await CryptoChain.verifyChain(session.entries);html+=`<div class='verify-session ${result.valid?'verify-valid':'verify-invalid'}'><div>${s.platform}</div><div class='verify-session-status ${result.valid?'valid':'invalid'}'>${result.valid?'Chain intact':'Tampering detected'}</div></div>`;} verifyResults.innerHTML=html||'<div class="empty-state">No sessions</div>';verifyModal.classList.remove('hidden');};closeModal.onclick=()=>verifyModal.classList.add('hidden');clearAll.onclick=async()=>{const sessions=await SessionStorage.getAllSessions();for(const s of sessions)await SessionStorage.deleteSession(s.sessionId);await refreshAll();};window.addEventListener('unload',()=>clearInterval(timer));init();})();